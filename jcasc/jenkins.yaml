jenkins:
  systemMessage: "Jenkins configuracion inicial"
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: admin
          password: password
  # clouds:
  #   - docker:
  #       name: 'dockercito'
  #       dockerApi:
  #         dockerHost:
  #           uri: "tcp://dind:2375"
  #         # exposeDockerHost: true
